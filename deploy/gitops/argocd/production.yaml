apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: hotel-booking-cancellation-prediction-production
  namespace: argocd
  labels:
    environment: production
  annotations:
    notifications.argoproj.io/subscribe.on-sync-succeeded.slack: ds-deployments
    notifications.argoproj.io/subscribe.on-sync-failed.slack: ds-deployments
    notifications.argoproj.io/subscribe.on-health-degraded.pagerduty: ds-critical
spec:
  project: hotel-booking-cancellation-prediction

  source:
    repoURL: https://github.com/your-org/hotel-booking-cancellation-prediction.git
    targetRevision: main
    path: deploy/helm/hotel-booking-cancellation-prediction
    helm:
      valueFiles:
        - values.yaml
        - values-production.yaml

  destination:
    server: https://kubernetes.default.svc
    namespace: hotel-booking-cancellation-prediction

  syncPolicy:
    # Production requires manual sync (approve step)
    automated: null
    syncOptions:
      - CreateNamespace=true
      - PruneLast=true
      - ApplyOutOfSyncOnly=true
    retry:
      limit: 5
      backoff:
        duration: 10s
        factor: 2
        maxDuration: 5m

  ignoreDifferences:
    - group: apps
      kind: Deployment
      jsonPointers:
        - /spec/replicas
