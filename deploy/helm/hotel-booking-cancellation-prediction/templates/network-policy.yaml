{{- if .Values.networkPolicy.enabled }}
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ include "hotel-booking-cancellation-prediction.fullname" . }}-restrict
  namespace: {{ include "hotel-booking-cancellation-prediction.namespace" . }}
  labels:
    {{- include "hotel-booking-cancellation-prediction.labels" . | nindent 4 }}
spec:
  podSelector:
    matchLabels:
      {{- include "hotel-booking-cancellation-prediction.selectorLabels" . | nindent 6 }}
  policyTypes:
    - Ingress
    - Egress
  ingress:
    - from:
        - namespaceSelector:
            matchLabels:
              {{- toYaml .Values.networkPolicy.ingressNamespaceSelector | nindent 14 }}
      ports:
        - protocol: TCP
          port: {{ .Values.service.targetPort }}
  egress:
    {{- range .Values.networkPolicy.egressPorts }}
    - to:
        - namespaceSelector: {}
      ports:
        - protocol: {{ .protocol }}
          port: {{ .port }}
    {{- end }}
{{- end }}
