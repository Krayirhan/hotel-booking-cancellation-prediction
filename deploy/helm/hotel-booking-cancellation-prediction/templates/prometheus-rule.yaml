{{- if .Values.monitoring.prometheusRule.enabled }}
apiVersion: monitoring.coreos.com/v1
kind: PrometheusRule
metadata:
  name: {{ include "hotel-booking-cancellation-prediction.fullname" . }}-rules
  namespace: {{ include "hotel-booking-cancellation-prediction.namespace" . }}
  labels:
    {{- include "hotel-booking-cancellation-prediction.labels" . | nindent 4 }}
spec:
  groups:
    - name: {{ include "hotel-booking-cancellation-prediction.fullname" . }}
      rules:
        {{- range .Values.monitoring.prometheusRule.rules }}
        - alert: {{ .alert }}
          expr: |-
{{ .expr | nindent 12 }}
          for: {{ .for }}
          labels:
            severity: {{ .severity }}
            service: {{ include "hotel-booking-cancellation-prediction.fullname" $ }}
          annotations:
            summary: {{ .summary | quote }}
            description: {{ .description | quote }}
        {{- end }}
{{- end }}
