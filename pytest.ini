[pytest]
testpaths = tests
addopts = -q
asyncio_mode = auto
markers =
    asyncio: async test support marker
    contract: API/version contract regression guards
    integration: integration tests requiring external resources
    slow: long-running tests

